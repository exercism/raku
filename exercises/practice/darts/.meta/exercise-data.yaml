methods: score
properties:
  score:
    test: |-
      sprintf(q:to/END/, |(%case<input><x y>:p).map(*.raku), %case<expected>, %case<description>.raku);
      cmp-ok(
          Darts.new( %s, %s ).score,
          &infix:<==>,
          %s,
          %s,
      );
      END


unit: class
example: |-
  has Rat() ($.x, $.y);

  method score {
  }

stub: |-
  has Rat() ($.x, $.y);

  method score {
  }
