properties:
  total:
    test: |-
      sprintf(q:to/END/, (%case<input><basket><>, |%case<expected description>).map(*.raku));
      is-deeply(
          best-price(%s),
          Rat.new(%s, 100),
          %s,
      );
      END

unit: module
example: |-
  sub best-price (@basket) is export {
  }

stub: |-
  sub best-price (@basket) is export {
  }
